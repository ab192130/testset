extends ../layout

block content
    h1= login
    pre
     .userInfo= 'ID: ' + login
     .userInfo= 'Pass: ' + pass

    - if (own)
        .userPanel You are logged in
        a#signOutBtn(href="#") Sign Out

extends ../layout

block content
    //- if (own)
        //include ..\partials\menu

    h1= user.name
    .panel.panel-default
      .panel-body
        ul.list-group
          li.list-group-item
            .userInfo= 'ID: ' + user._id
          li.list-group-item
            .userInfo= 'Password: ' + user.pass
          - if (user.email)
            li.list-group-item
              .userInfo= 'Email: ' + user.email




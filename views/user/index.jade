extends ../layout

block content
    - if (own)
        include ..\partials\menu

    h1= user.name
    pre
     .userInfo= 'ID: ' + user._id
     .userInfo= 'Pass: ' + user.pass
     - if (user.email)
       .userInfo= 'Email: ' + user.email


